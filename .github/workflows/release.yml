name: Release build

on:
 schedule:
    - cron: '*/5 * * * *'
 workflow_dispatch:
    inputs:
      name: 
        description: "Want to inject something to build?"
        default: "no"

    
jobs:
 tag:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    
    - uses: actions/checkout@v4

    - name: Bump version and push tag
      uses: anothrNick/github-tag-action@v1 # Don't use @master or @v1 unless you're happy to test the latest version
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # if you don't want to set write permissions use a PAT token
        WITH_V: false


  #   - name: conventional-release
  # # You may pin to the exact commit or the version.
  # # uses: mgoltzsche/conventional-release@639a16c599f21ca812f91d99bb759eec0386f73c
  #     uses: mgoltzsche/conventional-release@v0.7.4

    
#adding a comment to fix the ci workflow

    
    # - name: Hello Step
    #   run: echo "Hello ${{ github.event.inputs.name }}"
    
    # - name: Github Tag Bump
    #   uses: anothrNick/github-tag-action@1.67.0
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #     WITH_V: false
    #   id: tag_bump

    # - name: Set New Tag as Env Variable
    #   run: echo "NEW_TAG=${{ steps.tag_bump.outputs.new_tag }}" >> $GITHUB_ENV

    # - name: GH Release
    #   uses: softprops/action-gh-release@v2.0.4
    #   with:
    #    tag_name:  ${{ env.NEW_TAG }}
    #    generate_release_notes: true
    #    draft: false
    #    prerelease: false
         
